# Alternância mock/API real. Padrão (sem definir): usa mocks.
# Defina 'false' para chamar o backend Nest + Neon.
VITE_USE_MOCKS=true

# Base da API real (usada apenas quando VITE_USE_MOCKS=false)
VITE_API_URL=http://localhost:3333/api

# Tenant demo (seed Neon — slug "demo"). Opcional em rotas públicas.
# VITE_TENANT_ID=cmpug8cr60000max45xpcvyz7
